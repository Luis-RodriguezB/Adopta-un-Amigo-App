const initialState = {
  dogs: [],
  error: null,
  loading: false,
};

const DogsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default DogsReducer;
