import React from "react";
import { Text, View } from "native-base";

const Dogs = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Perros</Text>
    </View>
  );
};

export default Dogs;
